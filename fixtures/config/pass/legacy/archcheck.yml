version: 1

modules:
  ui:
    paths: ["src/ui/**"]
  storage:
    paths: ["src/storage/**"]
  net:
    paths: ["src/net/**"]

rules:
  - type: forbidden
    name: storage-no-ui
    from: [storage]
    to:   [ui]

  - type: forbidden
    name: net-no-ui
    from: [net]
    to:   [ui]
