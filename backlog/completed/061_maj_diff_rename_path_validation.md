# [SCAN] #056 --diff: rename-detection (FP-other фикс)

**Дата создания:** 2026-05-31
**Статус:** wip
**Модуль:** SCAN
**Приоритет:** major
**Related:** #060 (валидация), #056

## Цель
Убрать FP-other (~17% в iter1): `--diff` репортит переименованный файл как
ADDED-новый-путь ⟵ BASE-старый-путь (тот же файл переехал, не дубль).

## Фикс
`changedSourceFiles`: `git diff --numstat` → `git diff -M --numstat`. Rename
показывается как `old => new` и отсекается существующим кодом → файл не
обрабатывается, фантомная пара не рождается.

## Сделано
- [x] `-M` добавлен (main.cpp ~839), #056 пересобран 22:10.

## Проверка
- [ ] iter2: пары с rename-паттерном (same basename, diff dir) исчезли/упали.

## Итог
**Статус:** completed — `git diff -M --numstat` добавлен (main.cpp), фантомные
rename-пары не рождаются. Проверка выполнена в рамках #060 iter2 (коммит 0beb2f7)
с уточнением: большинство same-basename пар оказались настоящими копиями, не
git-rename (то есть фикс корректен, а класс FP-other был меньше оценки iter1).
**Дата завершения:** 2026-06-11 (фактически — ранее; закрыта при проходе бэклога)
