# [RULES] GodHeader: структурные заголовки библиотек не должны репортиться как нарушения

**Дата создания:** 2026-05-28
**Дата старта:** —
**Статус:** new
**Модуль:** RULES
**Приоритет:** minor
**Сложность:** S
**Блокирует:** —
**Заблокирован:** —
**Related:** #028 (rules_engine_mvp), #031 (godheader_pch_exclusion)

## Цель

Уменьшить шум от GodHeader на крупных библиотеках где высокий fan-in — архитектурная норма, а не дефект.

## Контекст

Прогоны на OSS-проектах выявили категорию "структурных god-headers":

| Файл | Fan-in | Характер |
|------|--------|----------|
| `absl/base/config.h` | **507** | Фундаментальный конфиг всей Abseil. 508 прямых include. По смыслу — `<stddef.h>` |
| `absl/base/attributes.h` | 191 | Compiler attribute macros — нужны везде |
| `catch_test_macros.hpp` | 108 | Главный макро-заголовок тест-фреймворка — обязан быть в каждом тесте |
| `doctest_compatibility.h` | 77 | Тест-compat shim — аналогично |
| `spdlog/common.h` | 38 | Базовые типы spdlog — возможный реальный кандидат |

Порог 30 правильный для пользовательского кода, но слишком мал для "platform headers" и "test macro headers" в библиотеках.

Три возможных подхода:

**А — поднять дефолтный порог** (например, до 50):
Уберёт шум на abseil, не поломает детектирование `spdlog/common.h` (fan-in 38 < 50). Простейший фикс.

**Б — настраиваемый порог в конфиге:**
```yaml
rules:
  lakos_god_header:
    threshold: 50
```
Правильно, но требует конфига.

**В — exclude по паттерну:**
```yaml
rules:
  lakos_god_header:
    exclude: ["absl/base/config.h", "**/detail/config.hpp"]
```
Пользователь сам решает что считать допустимым god-header.

**Рекомендация:** для MVP — поднять дефолтный порог до 50 (вариант А). При появлении конфига добавить Б.

## План

- [ ] Обсудить правильный дефолт (30 vs 50 vs другой)
- [ ] `lakos_god_headers.h`: поменять `threshold_` default
- [ ] Проверить: abseil → 0 GodHeader (или ≤ разумное число)
- [ ] Проверить: `spdlog/common.h` (fan-in 38) — всё ещё детектируется при пороге 50? (нет → обсудить)
- [ ] Обновить документацию правила

## Сделано

- (пусто)

## Изменённые файлы

| Файл | Изменение |
|------|-----------|
| `include/archcheck/rules/lakos_god_headers.h` | дефолтный threshold |
| `tests/unit/rules/lakos_god_headers_test.cpp` | обновить тесты под новый дефолт |
